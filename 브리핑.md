# 프로젝트 브리핑 — 한양YK인터칼리지 2026 융합전공 소개행사 사전예약 및 체크인 관리 시스템

---

## 1. 프로젝트 컨텍스트

**행사명**: 융합 Universe : 6개 융합의 별을 연결하다  
**행사일시**: 2026. 5. 8.(금) 09:30~17:00  
**행사장소**: 한양종합기술원(HIT) 1층 양민용 커리어라운지

한양YK인터칼리지학부의 2026-1학기 전공 탐색 프로그램. 학생들이 6개 융합전공 부스를 사전예약하고 당일 체크인하는 전체 운영 시스템을 Google Apps Script + Firebase Firestore + GitHub Pages 조합으로 구축.

**접속 URL**:
- 예약페이지: `https://tinyurl.com/hicmajors` → `https://jyhan71-hue.github.io/hicmajors/`
- GAS 웹앱 URL: `https://script.google.com/macros/s/AKfycbziDcRyZD1lbvabjjOWB_WCGFuCutFUjiJFGvxAbKX3nUD9WAFpS1rZPtDrN81IG6fiMA/exec` (배포 @91, 현재 활성)
- Firebase 프로젝트 ID: `hic-major-booking-656ca` (`asia-northeast3`)
- GitHub 레포: `jyhan71-hue/hicmajors` (branch: main)

---

## 2. 확정된 결정사항 (아키텍처)

- **데이터 저장**: Firestore 먼저 저장 → `gasNotify()` fetch POST → GAS `doPost()` → Google Sheets + 메일
- **동기화 방식**: doPost 방식 확정 (1분 트리거 REST API는 Firebase IAM 403 오류로 폐기)
- **gasNotify**: fetch 우선 (응답 로그 가능), CORS 실패 시 sendBeacon 폴백
- **index.html**: GitHub Pages 서빙 → `google.script.run` 사용 불가
- **메일**: reserve → `sendConfirmMail`, change → `sendChangeMail`, cancel → `sendCancelMail`

---

## 3. 완료된 작업 전체 (현재 커밋 기준)

### 최신 추가 완료

- **부스 슬롯 정원 기능**: Settings 시트 `부스슬롯정원` 키(정수) → `_getSettings()` 파싱 → `doGet config` `boothSlotCapacity` 반환 → `SITE_CONFIG.boothSlotCapacity` 저장 → `getAvailableSlots`에서 Map 기반 카운트로 정원 미만인 슬롯만 표시; `submitReservation` / `changeReservation` / GAS `doPost reserve` / `doPost change` 모두 `taken >= capacity` 조건 검증. 키 없으면 기본값 1 유지.
- **부스 슬롯 드롭다운 잔여석 표시**: 정원 2명 이상일 때 `14:00 ~ 14:15 (잔여 N명)` 형식으로 표시. 정원 1명이면 기존과 동일하게 시간만 표시.
- **Admin 부스 사전예약 명단 출력**: admin.html 부스 탭 헤더에 "🖨️ 전체 출력", 부스 현황 섹션에 "🖨️ 현재 프로그램 출력" 버튼 추가. 취소 제외 예약자를 시간순 정렬해 프로그램별 테이블로 인쇄. `@media print` + `booth-print-mode` 클래스로 다중 페이지 출력 지원 (`position:static` 오버라이드).
- **Admin 설명회 체크인 전체 명단 출력**: admin.html 체크인 탭 헤더에 "🖨️ 전체 체크인 명단 출력" 버튼 추가. CheckIns 시트 기반, 학번 중복 제거(최초 체크인만 유효), 이름·학번·학과·체크인시각 컬럼으로 출력. GAS `getAllCheckedInStudents(password)` 신규 함수.
- **인터칼리지학부 부스 시간 제한 전 경로 봉쇄**: 신규예약 제출 시점(Firestore 쓰기 직전), 예약변경 제출 시점, GAS `doPost reserve`, GAS `doPost change` 4개 레이어 모두 서버·클라이언트 검증 추가. 편집 패널 슬롯 조회 시 `editDept` 값을 `overrideDept`로 전달하여 IC 필터 정상 적용.
- **staff.html 태블릿 가로 최적화**: 768px↑ 미디어쿼리 적용, 카드 최대 너비 900px, 폰트 업스케일.

### 이전 추가 완료 (브리핑 이후)
- **사전예약 현황 HIC/타과생 구분**: `getPreReservationStats` — 프로그램별 ic/other + 총 인원 행도 HIC·타과 분리 표시
- **Settings 시트 `설명회정원` 동적 조정**: SESSION_CAPACITY를 시트로 관리
- **이메일 일시·장소 Settings 연동**: `sendConfirmMail`, `sendChangeMail`, `_buildCertificateHtml` — 하드코딩 제거
- **기념품 1개 이상 허용 팝업**: 부스 1개 이상 시 확인 팝업 → 지급 가능, 0개면 완전 차단
- **참여확인서 부스 1개↑ 자격**: stampCount ≥ 1로 완화 (기존 ≥ 3)
- **부스 참석자 탭**: admin officeCard에 탭 추가 — GiftReceipts 기반, 프로그램별 기념품 수령자+서명 표시 및 서명부 PDF 생성
- **BlockedSlots → index.html 연동**: `doGet?page=config`에 `blockedSlots`(프로그램별 차단 목록) + `boothSlotRange` 포함 → index.html `getAvailableSlots`에서 필터링
- **부스 슬롯 시간대 Settings 연동**: `부스시작시간` / `부스종료시간` 키로 동적 제어 (기본 14:00~17:00)
- **슬롯 관리 저장 버튼**: 체크박스 = 차단 상태, 💾 저장 클릭 시 `saveSlotConfig` GAS 함수로 BlockedSlots 일괄 저장; 전체 차단/해제 편의 버튼 유지
- **인터칼리지학부 부스 시간 제한**: Settings 시트 `인터칼리지제한시작` / `인터칼리지제한종료` → doGet config에 `icBlockRange` 포함 → index.html 슬롯 필터 + Code.js doPost 서버 검증
- **인적사항 미완성 시 선택 비활성화**: 성명·학번·학과·연락처·이메일 5개 모두 입력 후에만 설명회·부스 선택 표시 (`display:none` 방식, IC 체크박스 변경 시도 연동)
- **Settings 시트 시간값 파싱 버그 수정**: `부스시작시간` 등 시간값 셀이 Sheets에서 Date로 자동변환 → `toTimeStr()` 사용으로 수정



- 부스 예약 / 설명회 예약 / 취소 / 변경 전체 흐름 구현
- 예약 비즈니스 규칙 5가지 (중복/시간겹침/LDC학과/설명회중복/기존예약 차단)
- Settings 시트 기반 동적 설정 (`_getSettings()`)
- **doGet `page=config` 분기 추가** (누락으로 인한 페이지 로드 불가 버그 수정)
- **gasNotify sendBeacon → fetch 전환** (GAS redirect 미추적 문제 해결)
- **testMailAndPost `Session.getActiveUser` → `Session.getEffectiveUser` 수정**
- 편집폼 시간 중복 슬롯 차단 (for...of 순차 로딩 + post-load 재필터)
- staff.html: 학번 즉시 판별 / 모드변경 비밀번호 인증
- warmup 트리거 (30분), sendRemindMails 트리거 (2026-05-07 09:00) 설정
- **체크인 마감 기능** (세션별 독립 관리): `ciRenderWalkStatus` 단방향 설계, 마감 시 시각적 배너 표시
- **walkInBlocked 구형 포맷 호환** + `_allowServerUnblock` 세션 전환 처리
- **admin.html 사전예약 현황 아코디언**: 행 클릭 시 세션(성명·학번·학과) / 부스(시간·성명·학번·학과·상담내용 보기버튼) 인라인 펼치기
- **admin.html 부스관리자 설명회 현황**: `loadSessPreReg()` — 본인 담당 프로그램의 설명회 사전예약 총원 + 인터칼리지학부/타과생 구분 + 리스트(5명 초과 시 스크롤, flex 레이아웃으로 헤더 고정)
- **GAS 함수 추가**: `getPreReservationDetail(password, type, program)` (전체관리 전용), `getSessionPreRegForProgram(password, program)` (전체관리 또는 해당 프로그램 관리자)
- **SESSION_CAPACITY 동적 조정**: Settings 시트 `설명회정원` 항목으로 관리 (없으면 기본값 30 유지)

---

## 4. 활성 제약사항

### 배포 규칙
- `index.html` 수정 → `git push`만으로 GitHub Pages 자동 반영
- `Code.js` / `staff.html` / `admin.html` 수정 → `clasp push` + **GAS 재배포 필수**
- **재배포 방법**: 배포 관리 → 최신 배포(@91 등) ✏️ → 새 버전 → 배포 (**신규 배포 생성 금지** — URL 바뀜)
- 재배포 후 `clasp deployments`로 최신 ID 확인 → `index.html` URL과 비교 → 다르면 교체 후 `git push`
- `clasp push` 전 반드시 `node --check Code.js` 실행

### 트리거 현황 (2026-04-14 기준)
- `warmup`: 30분 간격 (행사 후 자동 삭제)
- `sendRemindMails`: 2026-05-07 09:00 KST 1회
- `syncFirestoreToSheets` / `autoSyncFirebaseToSheets`: 폐기됨
- 초기화 필요 시 GAS 편집기에서 `fixAllTriggers()` 실행

### 예약 비즈니스 규칙 (5가지)
1. 동일 부스 학번당 하나의 시간대만
2. 기존 예약 있으면 신규 차단 → 변경 모드 유도
3. 동일 시간대 여러 부스 중복 불가
4. LDC 부스는 한양인터칼리지학부만
5. 설명회 프로그램당 1회만

---

## 4-2. 추가 제약사항 (최근 확인)

- **BoothReservations 시트 직접 편집 → 취소 메일 미발송**: `sendCancelMail`은 학생이 예약 페이지에서 취소 시에만 호출. 관리자가 시트 직접 수정 시 메일 없음
- **Settings 시트 시간값 입력 주의**: `부스시작시간`, `인터칼리지제한시작` 등 시간 키의 B열 값을 Sheets가 Date로 자동변환함 → `toTimeStr()`로 파싱 (코드에 반영됨, 입력 방식 무관)
- **슬롯 차단 저장은 반드시 💾 저장 버튼 클릭**: 체크박스만 바꾸고 저장 안 하면 반영 안 됨

## 5. 다음 단계

1. **Settings 시트 필수 항목 확인**:
   - `설명회정원` | `35` (또는 현재 정원)
   - `부스슬롯정원` | `5` (슬롯당 최대 신청 인원, 1이면 생략 가능)
   - `인터칼리지제한시작` | `14:00`
   - `인터칼리지제한종료` | `15:00`
   - `부스시작시간` / `부스종료시간` (필요 시)
2. **슬롯 차단 설정**: admin 부스관리자 페이지 → 슬롯 관리 탭 → 체크박스로 차단 설정 → 💾 저장
3. **행사 당일**: Settings 시트 `currentSessionIdx = 0` 확인 후 staff.html 운영

---

## 6. 시트 스키마

| 시트 | 주요 컬럼 |
|------|-----------|
| `BoothReservations` | [0]이름 [1]학번 [2]학과 [3]이메일 [4]연락처 [5]프로그램 [6]예약시간(Date) [7]상담내용 [8]서명 [9]상태 |
| `SessionPreReg` | [0]이름 [1]학번 [2]학과 [3]연락처 [4]이메일 [5]설명회명 [6]등록일시 |
| `Settings` | A=항목명, B=값 / 주요 키: `예약오픈여부`, `설명회세션`, `부스프로그램`, `설명회정원`, `부스슬롯정원`, `currentSessionIdx`, `부스시작시간`, `부스종료시간`, `인터칼리지제한시작`, `인터칼리지제한종료`, `행사일시`, `행사장소` |
| `BlockedSlots` | [0]프로그램 [1]시간(Date 자동변환 주의) [2]차단여부(TRUE/FALSE) |
| `GiftReceipts` | [0]수령시각 [1]학번 [2]이름 [3]학과 [4]방문부스(콤마구분) [5]스탬프수 [6]가산점과목 [7]서명 [8]검증코드 |
